# Array Explorer Lab — For-Loop & Basic Array Methods

## 📌 Purpose

The purpose of this lab is to review and strengthen foundational JavaScript skills using **arrays** and **for-loops**, without relying on advanced built-in array methods. Learn how to display values, manipulate arrays, and practice logical thinking using only basic tools.

---

## 🎯 Learning Objectives

- Use **for-loop** or **for-each** to iterate through arrays and display values.
- Write a **reverse for-loop** to show arrays from last to first.
- Demonstrate understanding of basic array manipulation:
  - `push()`
  - `pop()`
  - `shift()`
  - `unshift()`
- Update and modify arrays through custom JavaScript functions.
- Output results to the webpage using the DOM.
- Organize code into functions that handle specific tasks.
- Work with user input to add and remove array elements.

---

## 📋 Project Requirements

### **Array Requirements**

- Your array must contain **a minimum of 7 values**.
- The array must be visible on the page through a display function.

### **Function Requirements**

Your project must include **at least 6 separate JavaScript functions**, such as:

1. A function to display the array using a **for-loop**
2. A function to display the array **in reverse** using a for-loop
3. A function that uses **push()**
4. A function that uses **unshift()**
5. A function that uses **pop()**
6. A function that uses **shift()**
7. (Optional) A function to refresh the output display
8. (Optional) A function to validate user input

---

## 🚫 Restrictions

To ensure proper skill development, the following are **not allowed**:

- `map()`
- `filter()`
- `reduce()`

---

## 📤 Output Requirements

Your webpage must:

- Display the **current array**
- Show the results of each function after it is run
- Update a visible output area using:
  - `.innerText`
  - or `.innerHTML`

---

## ✔ Completion Checklist

- [ ] At least **6 functions created**
- [ ] Forward for-loop works correctly
- [ ] Reverse for-loop works correctly
- [ ] push, pop, shift, and unshift used properly
- [ ] Output displays correctly
